import { MapaSelectorDirective } from './mapa-selector.directive';

describe('MapaSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new MapaSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
